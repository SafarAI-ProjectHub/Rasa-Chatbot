# This file contains the different endpoints your bot can use.

# ---------------------------
# Rasa X (Model Server)
# ---------------------------
models:
  url: http://localhost:5002/api/projects/default/models/tags/production
  token: rasax-token
  wait_time_between_pulls: 10


# ---------------------------
# Action Server (local)
# ---------------------------
action_endpoint:
  url: "http://localhost:5055/webhook"


# ---------------------------
# Tracker Store (اختياري الآن)
# إذا لم نربطه بـ Postgres → احذفه
# ---------------------------
# tracker_store:
#   type: "sql"
#   dialect: "postgresql"
#   host: "localhost"
#   port: 5432
#   db: "rasa"
#   username: "rasa"
#   password: "rasa"


# ---------------------------
# Event Broker (اختياري الآن)
# ---------------------------
# event_broker:
#   type: "pika"
#   url: "localhost"
#   port: 5672
#   username: "guest"
#   password: "guest"
