# This file contains the different endpoints your bot can use.
# Docker setup: use docker service names (NOT localhost)

models:
  url: http://rasax:5002/api/projects/default/models/tags/production
  token: PUT_RASAX_API_TOKEN_HERE
  wait_time_between_pulls: 10

action_endpoint:
  url: "http://rasa_actions:5055/webhook"
